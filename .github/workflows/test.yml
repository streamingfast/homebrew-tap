name: Test

# `test` is a required status check on master. It must run on every pull request —
# no path filters — or a pull request that touches no formula never reports it and
# can never be merged.
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate formulae
        run: ruby .github/scripts/check-formulae.rb

  style:
    runs-on: ubuntu-latest
    # Advisory only: the generated formulae do not satisfy `brew style`, so this
    # reports without gating. Not a required check.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: brew style
        run: brew style ./*.rb
